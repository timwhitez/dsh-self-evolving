# v0.1.1 migration baseline

This candidate starts the `dsh-self-evolving-candidate-tree-v2` lineage. Its executable
`src/index.ts` is byte-identical to the accepted v0.1 baseline; only trusted protocol metadata and package identity
change. No v0.1 result or failure pool is inherited.
