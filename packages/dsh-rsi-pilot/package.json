{
  "name": "@dsh-rsi/pilot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Gate 6 pilot search loop driver: autonomously runs propose→build→evaluate for K=10 admitted candidates on the dev set, with crash/resume, dedup, and full evidence. Dev-only (no sealed reveal). Reuses the proposer + builder + search + controller modules.",
  "main": "lib/index.js",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "default": "./lib/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf lib .tsbuildinfo"
  },
  "dependencies": {
    "@dsh-rsi/candidate-sdk": "workspace:*",
    "@dsh-rsi/core": "workspace:*",
    "@dsh-rsi/search": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
