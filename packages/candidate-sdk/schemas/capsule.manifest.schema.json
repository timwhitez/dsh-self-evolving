{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://dsh-self-evolving/capsule.manifest.schema.json",
  "title": "Evaluation capsule manifest",
  "description": "Self-contained immutable capsule descriptor with a typed complete-tree checksum manifest (spec 02 §12).",
  "type": "object",
  "required": [
    "schemaVersion",
    "candidateId",
    "runtime",
    "candidate",
    "runner",
    "provenance",
    "sbom",
    "sha256sums"
  ],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": { "const": 2 },
    "candidateId": { "type": "string" },
    "runtime": {
      "type": "object",
      "required": ["kind", "ref"],
      "additionalProperties": false,
      "properties": {
        "kind": { "type": "string", "enum": ["pinned-closure", "install-manifest"] },
        "ref": { "type": "string" },
        "hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    },
    "candidate": {
      "type": "object",
      "required": ["bundleHash"],
      "additionalProperties": false,
      "properties": {
        "bundleHash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "buildCandidateId": { "type": "string", "pattern": "^c_[a-z2-7]{26}$" }
      }
    },
    "runner": {
      "type": "object",
      "required": ["overlay"],
      "additionalProperties": false,
      "properties": {
        "overlay": { "type": "string" },
        "hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    },
    "provenance": {
      "type": "object",
      "required": ["ref"],
      "additionalProperties": false,
      "properties": {
        "ref": { "type": "string" },
        "hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    },
    "sbom": {
      "type": "object",
      "required": ["ref"],
      "additionalProperties": false,
      "properties": {
        "ref": { "type": "string" },
        "hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    },
    "sha256sums": {
      "type": "object",
      "required": ["ref", "hash", "format"],
      "additionalProperties": false,
      "properties": {
        "ref": { "type": "string" },
        "hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
        "format": { "const": "dsh-capsule-tree-v2" }
      }
    }
  }
}
